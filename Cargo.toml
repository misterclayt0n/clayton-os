[package]
name = "clayton-os"
version = "0.1.0"
edition = "2021"

# for cargo build 
[profile.dev]
panic = "abort" # disable stack unwinding on panic

# for cargo build --release
[profile.release]
panic = "abort" 

[dependencies]
